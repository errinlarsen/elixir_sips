defmodule TestProject do
  @moduledoc """
    This is a **module**! I made it _myself_!!
  """
  @doc """
    ```publish(message)``` returns the message.

    * Way cool.
    * Also, this.
  """
  def publish(message) do
    message
  end
end
